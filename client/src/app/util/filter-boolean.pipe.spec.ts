import { util_filter_boolean_pipe } from './filter-boolean.pipe';

describe('FilterBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new util_filter_boolean_pipe();
    expect(pipe).toBeTruthy();
  });
});
